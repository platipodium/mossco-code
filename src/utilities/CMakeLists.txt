# This directory will contain utility code for the project.
# Add libraries or executables for each utility.
# Example:
# add_library(MyUtility MyUtility.cpp)
# target_include_directories(MyUtility PUBLIC ../include)
