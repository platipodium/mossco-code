# This generic coupling specification
# couples the netcdf input to the netcdf output component

instances:
  - restart: netcdf_input

coupling:
  - components:
     - restart
     - netcdf
    interval: 1 h
